server:
  port: 8080

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5433/minicommerce
    username: miniuser
    password: minipass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: jwt123
  expiration: 3600